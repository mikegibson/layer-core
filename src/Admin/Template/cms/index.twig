{% extends "@admin/layout/admin.twig" %}
{% table_theme '@admin/table/index.twig' %}

{% block content %}

	<h1>{{ dataType.pluralHumanName }}</h1>

	{{ admin_link( dataType ) }}

	{{ link( 'List link', 'admin_scaffold', {'namespace': dataType.namespace, 'type': dataType.slug, 'action': 'index'}) }}

	{{ table( paginator ) }}

	{{ paginator.total }}

{% endblock %}